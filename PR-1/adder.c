#include "file.h"
#include "adder.h"
#include <stdio.h>

void add_queue(char* path, char* data, char* struc)
{
	printf("path: %s\ndata: %s\nstruct: %s", path, data, struc);
}

void add_set(char* path, char* data, char* struc)
{

}

void add_ht(char* path, char* data, char* struc)
{

}